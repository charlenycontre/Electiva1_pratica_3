describe('Página estática', () => {
  test('El título de la página es correcto', () => {
    const titulo = 'Landing Page - Start Bootstrap Theme';
    expect(titulo).toBe('Landing Page - Start Bootstrap Theme');
  });
});
